# GPU-only training deps. Install these on the Colab/RunPod/Lambda box, NOT on
# the CPU serving machine. Torch must match the box's CUDA — the line below is
# the default CUDA wheel; on Colab torch is preinstalled, so you can skip it.
torch>=2.3
transformers>=4.45
peft>=0.13
trl>=0.11
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.44   # 4-bit QLoRA; CUDA-only
sentencepiece>=0.2
# curate_dataset.py and the eval scorer need nothing beyond the base repo deps.
