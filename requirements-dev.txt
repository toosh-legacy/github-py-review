# Everything a contributor needs: the runtime, plus lint and tests.
#
#     pip install -r requirements-dev.txt
#
# CI installs this file; the Docker image installs requirements.txt only.

-r requirements.txt

ruff==0.8.6
pytest==9.1.1
