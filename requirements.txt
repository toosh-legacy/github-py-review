# Runtime dependencies, pinned. This is what the Docker image and CI install;
# pyproject.toml carries the same set as ranges for `pip install`.
#
# The scanner checks these pins against OSV on every CI run (the self-scan job),
# so a published advisory here breaks the build — which is the point of shipping
# a dependency detector.
#
#   contributors:  pip install -r requirements-dev.txt
#   users:         pip install repo-security-scanner

pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1             # the OSV client
bandit==1.9.4             # unsafe-code detector for Python

# Optional, triage only. Without it the scanner runs detection and ships the
# rule-authored explanations instead of model-written ones.
openai==1.59.6

# The JS/TS detector needs Node, not pip: run `npm install` in
# src/reposec/detectors/eslint/ to enable eslint-plugin-security. Without it the
# scanner falls back to regex checks and reports the detector as degraded.
# The Docker image installs it for you.
