# Dependencies for the Streamlit dashboard only.
#
# It is deployed separately from the API (Streamlit Community Cloud, or any host
# that runs `streamlit run`), so it keeps its own file and the API image does
# not have to carry Streamlit.
#
#     BACKEND_URL=https://your-api streamlit run src/apps/dashboard/app.py

streamlit==1.61.1
requests==2.34.2
